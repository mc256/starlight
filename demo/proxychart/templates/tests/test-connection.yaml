apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "proxychart.fullname" . }}-test-connection"
  labels:
    {{- include "proxychart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "proxychart.fullname" . }}:{{ .Values.service.port }}']
      resources:
        limits:
          cpu: 0.5
          memory: 128Mi
        requests:
          cpu: 0.5
          memory: 128Mi
  restartPolicy: Never
