apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "starlight-proxy-chart.fullname" . }}-test-connection"
  labels:
    {{- include "starlight-proxy-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "starlight-proxy-chart.fullname" . }}:{{ .Values.service.port }}']
      resources:
        limits:
          cpu: 0.5
          memory: 128Mi
        requests:
          cpu: 0.5
          memory: 128Mi
  restartPolicy: Never
